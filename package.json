{
  "scripts": {
    "start": "node .",
    "dev": "nodemon --ext js . --exec \"npm run start\"",
    "up": "docker-compose up --build --force-recreate"
  },
  "dependencies": {
    "body-parser": "^1.19.1",
    "express": "^4.17.2",
    "highcharts-export-server": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
